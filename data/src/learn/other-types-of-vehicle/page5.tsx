const title = `Trams`;
const content = `
Trams operate in some cities. Take extra care because they:
Are silent.
Move quickly.
Cannot steer to avoid you.
In such cities, additional white light signals at some traffic lights are specifically for tram drivers.

`;

export default {
    title,
    content,
};
